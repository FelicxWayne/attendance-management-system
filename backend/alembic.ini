# Alembic configuration for Attendance Management System

[alembic]
# Path to migration scripts
script_location = alembic

# Version locations
version_locations = %(here)s/alembic/versions

# Database URL (dynamically overridden by app.core.config.settings in env.py)
sqlalchemy.url = postgresql+psycopg://postgres:postgres@localhost:5432/attendance_db

[post_write_hooks]

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
